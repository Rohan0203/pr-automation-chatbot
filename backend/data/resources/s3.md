# S3 Resource — Agent Guide# S3 Resource — Agent Guide# S3 Resource — Agent Guide



## Agent Rules

- On the first collection turn, present ALL mandatory fields (that aren't

  auto-set) with their format, constraints, and allowed values.## Agent Rules## Agent Rules

- Tell the user: "You can provide all of these in one message, or one at a time."

- On subsequent turns, extract ALL provided fields, then show remaining mandatory fields.- On the first collection turn, present ALL mandatory fields (that aren't- On the first collection turn, present ALL remaining mandatory fields (that

- ALWAYS normalize field values BEFORE validating. For example, `Food` → `FOOD` → valid.

  Never reject a value that can be normalized to a valid one.  auto-set) with their format, constraints, and allowed values.  aren't auto-set) with their format, constraints, and allowed values

- If user provides multiple fields in one message, extract all of them first,

  then only ask for what is still missing — never re-ask a field already provided.- Tell the user: "You can provide all of these in one message, or one at a time."- Tell the user: "You can provide all of these in one message, or one at a time."

- Max 3 retries per field.

  - Mandatory field fails 3 retries → abort session:- On subsequent turns, extract ALL provided fields, then show remaining mandatory fields.- On subsequent turns, extract ALL provided fields, then show remaining mandatory fields

    "Cannot complete config without a valid <field>. Please restart with correct value."

- Never ask for auto-generated or auto-set fields.- ALWAYS normalize field values BEFORE validating. For example, `Food` → `FOOD` → valid.- After all mandatory fields are collected, present optional fields with their defaults

- Show confirmation summary before generating YAML.

- Reject unknown fields: "Field not recognized, will be excluded."  Never reject a value that can be normalized to a valid one.- If user provides multiple fields in one message, extract all of them first,

- There are NO optional fields for S3. Once all mandatory fields are collected,

  go straight to confirmation. Do NOT ask about optional or security fields.- If user provides multiple fields in one message, extract all of them first,  then only ask for what is still missing — never re-ask a field already provided



---  then only ask for what is still missing — never re-ask a field already provided.- Max 3 retries per field



## Fields- Max 3 retries per field.  - Mandatory field fails 3 retries → abort session:



| Field | Required | Default | Allowed / Validation |  - Mandatory field fails 3 retries → abort session:    "Cannot complete config without a valid <field>. Please restart with correct value."

|-------|----------|---------|----------------------|

| `intake_id` | ✅ | — | Non-empty string. Format: alphanumeric with optional hyphens (e.g. `I-123456`, `M0000440`, `M-0000485`, `M0000452`) |    "Cannot complete config without a valid <field>. Please restart with correct value."  - Optional field fails 3 retries → skip and note it as unresolved in YAML comment

| `bucket_name` | ✅ | — | 3–63 chars, lowercase letters + hyphens + numbers only, DNS-compliant, no uppercase, no underscores. Must start with `{env}-{type}` prefix (e.g. `dev-lh1-`, `prd-cmp1-`). Must end with a purpose suffix (`-src`, `-dp`, `-scripts`, `-eng-assets`). |

| `bucket_description` | ✅ | — | 1–256 chars, describes what data the bucket stores |- Never ask for auto-generated or auto-set fields.- Never ask for auto-generated or auto-set fields

| `aws_account_id` | ✅ | — | Must be one of the 10 allowed account IDs (see table below). Always single-quoted in YAML. |

| `aws_region` | Auto-set | `us-east-1` | Fixed value. Auto-set, always included in YAML. Never ask user. Mention once: "Region will be auto-set to us-east-1." |- Show confirmation summary before generating YAML.- Show confirmation summary before generating YAML

| `usage_type` | ✅ | — | Enum: `Source`, `DataProduct`, `Scripts`, `EngAssets` |

| `enterprise_or_func_name` | ✅ | — | Enum: `AGTR`, `CORP`, `FOOD`, `SPEC` |- Reject unknown fields: "Field not recognized, will be excluded."- Reject unknown fields: "Field not recognized, will be excluded"

| `enterprise_or_func_subgrp_name` | ✅ | — | Short uppercase string (e.g. `APAC`, `HR`, `FIN`, `ANH`, `DTD`). Can be empty string `""` if user has no subgroup. Always include in YAML. |

- There are NO optional fields for S3. Once all mandatory fields are collected,

**Total: 7 fields for user to provide + 1 auto-set. No optional fields.**

  go straight to confirmation. Do NOT ask about optional or security fields.---

---



## Allowed Account IDs

---## Fields

| Account ID | Alias | Env | Type | Enterprise |

|------------|-------|-----|------|------------|

| `438465132548` | minerva-dev-lakehouse-001 | dev | Lakehouse | Common (any enterprise) |

| `068887784423` | minerva-dev-compute-001 | dev | Compute | AGTR |## Fields| Field | Req | Default | Allowed / Validation |

| `933999308564` | minerva-dev-compute-002 | dev | Compute | FOOD |

| `836901248866` | minerva-dev-compute-003 | dev | Compute | SPEC ||-------|-----|---------|----------------------|

| `324612370323` | minerva-dev-compute-004 | dev | Compute | CORP |

| `578647603827` | minerva-prod-lakehouse-001 | prd | Lakehouse | Common (any enterprise) || Field | Required | Default | Allowed / Validation || `intake_id` | ✅ | — | Non-empty, format: alphanumeric with optional hyphens (e.g. `I-123456`, `M0000440`, `M-0000485`) |

| `367241115350` | minerva-prod-compute-001 | prd | Compute | AGTR |

| `884308299029` | minerva-prod-compute-002 | prd | Compute | FOOD ||-------|----------|---------|----------------------|| `bucket_name` | ✅ | — | 3–63 chars, lowercase + hyphens + numbers only, DNS-compliant, no uppercase, no underscores |

| `011379513867` | minerva-prod-compute-003 | prd | Compute | SPEC |

| `632247962242` | minerva-prod-compute-004 | prd | Compute | CORP || `intake_id` | ✅ | — | Non-empty string. Format: alphanumeric with optional hyphens (e.g. `I-123456`, `M0000440`, `M-0000485`, `M0000452`) || `bucket_description` | ✅ | — | 1–256 chars, describe what data the bucket stores |



If account ID not in this list → **Error: not a registered account.**| `bucket_name` | ✅ | — | 3–63 chars, lowercase letters + hyphens + numbers only, DNS-compliant, no uppercase, no underscores. Must start with `{env}-{type}` prefix (e.g. `dev-lh1-`, `prd-cmp1-`). Must end with a purpose suffix (`-src`, `-dp`, `-scripts`, `-eng-assets`). || `aws_account_id` | ✅ | — | Must be one of the allowed account IDs (see Account IDs below) — always single-quoted in YAML |



---| `bucket_description` | ✅ | — | 1–256 chars, describes what data the bucket stores || `aws_region` | ❌ | `us-east-1` | Fixed — auto-set, always included in YAML, never ask user, inform once |



## Usage Type Constraints| `aws_account_id` | ✅ | — | Must be one of the 10 allowed account IDs (see table below). Always single-quoted in YAML. || `usage_type` | ✅ | — | Enum: `Source`, `DataProduct`, `Scripts`, `EngAssets` |



| usage_type | Allowed Accounts || `aws_region` | Auto-set | `us-east-1` | Fixed value. Auto-set, always included in YAML. Never ask user. Mention once: "Region will be auto-set to us-east-1." || `enterprise_or_func_name` | ✅ | — | Enum: `AGTR`, `CORP`, `FOOD`, `SPEC` — hard reject if not in list |

|------------|-----------------|

| `Source` | Lakehouse only (`438465132548`, `578647603827`) || `usage_type` | ✅ | — | Enum: `Source`, `DataProduct`, `Scripts`, `EngAssets` || `enterprise_or_func_subgrp_name` | ❌ | `""` | Short uppercase string (e.g. `APAC`, `HR`, `FIN`). Always include in YAML even if empty. |

| `DataProduct` | Compute only (all 8 compute accounts) |

| `Scripts` | Any account (lakehouse or compute) || `enterprise_or_func_name` | ✅ | — | Enum: `AGTR`, `CORP`, `FOOD`, `SPEC` || `versioning_enabled` | ❌ | `false` | `true` / `false` |

| `EngAssets` | Any account (lakehouse or compute) |

| `enterprise_or_func_subgrp_name` | ✅ | — | Short uppercase string (e.g. `APAC`, `HR`, `FIN`, `ANH`, `DTD`). Can be empty string `""` if user has no subgroup. Always include in YAML. || `public_access_blocked` | ❌ | `true` | `true` / `false` |

Cross-validation note: The generator does NOT enforce usage_type ↔ account

constraints during collection (accept each field individually). The Reviewer| `encryption_enabled` | ❌ | `true` | `true` / `false` |

will catch mismatches later.

**Total: 7 fields for user to provide + 1 auto-set. No optional fields.**| `encryption_type` | ❌ | `SSE-S3` | Enum: `SSE-S3`, `SSE-KMS`, `SSE-C` |

---

| `encryption_key_arn` | Cond | — | Required if `encryption_type ≠ SSE-S3`. Format: `arn:aws:kms:<region>:<account>:key/<id>` |

## Scripts Auto-Behavior

---

When `usage_type = Scripts`:

- Automatically include `versioning_enabled: true` in the generated YAML.---

- Inform the user: "Versioning will be auto-enabled for Scripts buckets."

- Do NOT ask the user about versioning.## Allowed Account IDs



---## Allowed Account IDs



## Normalization Rules| Account ID | Alias | Env | Type | Enterprise |



All enum fields are **case-insensitive**. Normalize BEFORE validating.|------------|-------|-----|------|------------|### Dev Accounts



| Field | Input Examples | Normalize To || `438465132548` | minerva-dev-lakehouse-001 | dev | Lakehouse | Common (any enterprise) || Account ID | Alias |

|-------|---------------|--------------|

| `usage_type` | `source`, `SOURCE`, `src` | `Source` || `068887784423` | minerva-dev-compute-001 | dev | Compute | AGTR ||------------|-------|

| `usage_type` | `dataproduct`, `data product`, `dp`, `DP` | `DataProduct` |

| `usage_type` | `scripts`, `script`, `SCRIPTS` | `Scripts` || `933999308564` | minerva-dev-compute-002 | dev | Compute | FOOD || `438465132548` | minerva-dev-lakehouse-001 |

| `usage_type` | `engassets`, `eng assets`, `engineering assets`, `eng-assets` | `EngAssets` |

| `enterprise_or_func_name` | `agtr`, `Agtr`, `ag trading` | `AGTR` || `836901248866` | minerva-dev-compute-003 | dev | Compute | SPEC || `068887784423` | minerva-dev-compute-001 |

| `enterprise_or_func_name` | `corp`, `Corp`, `corporate` | `CORP` |

| `enterprise_or_func_name` | `food`, `Food`, `FOOD` | `FOOD` || `324612370323` | minerva-dev-compute-004 | dev | Compute | CORP || `933999308564` | minerva-dev-compute-002 |

| `enterprise_or_func_name` | `spec`, `Spec`, `specialized` | `SPEC` |

| `enterprise_or_func_subgrp_name` | `apac`, `Apac` | `APAC` || `578647603827` | minerva-prod-lakehouse-001 | prd | Lakehouse | Common (any enterprise) || `836901248866` | minerva-dev-compute-003 |

| `enterprise_or_func_subgrp_name` | `hr`, `Hr` | `HR` |

| `enterprise_or_func_subgrp_name` | `fin`, `Fin` | `FIN` || `367241115350` | minerva-prod-compute-001 | prd | Compute | AGTR || `324612370323` | minerva-dev-compute-004 |

| `aws_region` | `us east 1`, `US-EAST-1`, `useast1` | `us-east-1` |

| `884308299029` | minerva-prod-compute-002 | prd | Compute | FOOD |

**CRITICAL: Normalize first, then validate. `Food` → `FOOD` → valid. Do NOT reject `Food`.**

| `011379513867` | minerva-prod-compute-003 | prd | Compute | SPEC |### Prod Accounts

---

| `632247962242` | minerva-prod-compute-004 | prd | Compute | CORP || Account ID | Alias |

## YAML Generation Rules

|------------|-------|

1. **Field order:** `intake_id` → `bucket_name` → `bucket_description` → `aws_account_id` → `aws_region` → `usage_type` → `enterprise_or_func_name` → `enterprise_or_func_subgrp_name` → (versioning if Scripts)

2. Always include `enterprise_or_func_subgrp_name` even if empty string.If account ID not in this list → **Error: not a registered account.**| `578647603827` | minerva-prod-lakehouse-001 |

3. **Do NOT include** `versioning_enabled`, `public_access_blocked`, `encryption_enabled`, `encryption_type`, or `encryption_key_arn` — UNLESS `usage_type = Scripts` (then include `versioning_enabled: true` only).

4. **Quoting rules:**| `367241115350` | minerva-prod-compute-001 |

   - `aws_account_id` → always single-quoted: `'438465132548'`

   - Strings with spaces → double quotes: `"Stores data for..."`---| `884308299029` | minerva-prod-compute-002 |

   - Plain strings (no spaces) → no quotes: `AGTR`

   - Booleans → no quotes: `true` / `false`| `011379513867` | minerva-prod-compute-003 |

   - Empty string → double quotes: `""`

## Usage Type Constraints| `632247962242` | minerva-prod-compute-004 |

---



## Templates

| usage_type | Allowed Accounts |If the user provides an account ID not in these lists → **Error: not a registered account.**

### Lakehouse — Source Bucket

```yaml|------------|-----------------|

intake_id: M0000452

bucket_name: prd-lh1-agtr-src| `Source` | Lakehouse only (`438465132548`, `578647603827`) |---

bucket_description: Stores MIF to lakehouse Ingestion pattern SAP TCC objects

aws_account_id: '578647603827'| `DataProduct` | Compute only (all 8 compute accounts) |

aws_region: us-east-1

usage_type: Source| `Scripts` | Any account (lakehouse or compute) |## Dependency Rules

enterprise_or_func_name: AGTR

enterprise_or_func_subgrp_name: APAC| `EngAssets` | Any account (lakehouse or compute) |

```

| Condition | Action |

### Lakehouse — EngAssets Bucket

```yamlCross-validation note: The generator does NOT enforce usage_type ↔ account|-----------|--------|

intake_id: M0000001

bucket_name: prd-lh1-corp-dtd-eng-assetsconstraints during collection (accept each field individually). The Reviewer| `encryption_type = SSE-S3` | Omit `encryption_key_arn` from YAML |

bucket_description: "Stores Engineering Assets for CORP DTD"

aws_account_id: '578647603827'will catch mismatches later.| `encryption_type = SSE-KMS or SSE-C` | `encryption_key_arn` required — ask user |

aws_region: us-east-1

usage_type: EngAssets| `usage_type = Scripts` | Auto-set `versioning_enabled = true`, inform user |

enterprise_or_func_name: CORP

enterprise_or_func_subgrp_name: DTD---

```

---

### Lakehouse — Scripts Bucket

```yaml## Scripts Auto-Behavior

intake_id: M0000001

bucket_name: prd-lh1-corp-dtd-scripts## Normalization Rules

bucket_description: "Stores Scripts for CORP DTD"

aws_account_id: '578647603827'When `usage_type = Scripts`:Apply before validation. Silently normalize.

aws_region: us-east-1

usage_type: Scripts- Automatically include `versioning_enabled: true` in the generated YAML.

enterprise_or_func_name: CORP

enterprise_or_func_subgrp_name: DTD- Inform the user: "Versioning will be auto-enabled for Scripts buckets."| Field | User Input Examples | Normalize To |

versioning_enabled: true

```- Do NOT ask the user about versioning.|-------|-------------------|--------------|



### Compute — DataProduct Bucket| `aws_region` | `us east 1`, `US-EAST-1`, `useast1`, `us_east_1` | `us-east-1` |

```yaml

intake_id: M0000449---| `usage_type` | `source`, `SOURCE`, `src` | `Source` |

bucket_name: prd-cmp4-corp-fin-dp

bucket_description: Stores data product for Corporate Finance| `usage_type` | `dataproduct`, `data product`, `DataProduct`, `dp`, `DP` | `DataProduct` |

aws_account_id: '632247962242'

aws_region: us-east-1## Normalization Rules| `usage_type` | `scripts`, `script`, `SCRIPTS` | `Scripts` |

usage_type: DataProduct

enterprise_or_func_name: CORP| `usage_type` | `engassets`, `eng assets`, `EngAssets`, `engineering assets`, `eng-assets` | `EngAssets` |

enterprise_or_func_subgrp_name: FIN

```All enum fields are **case-insensitive**. Normalize BEFORE validating.| `enterprise_or_func_name` | `agtr`, `Agtr`, `AGTR` | `AGTR` |



### Compute — Scripts Bucket| `enterprise_or_func_name` | `corp`, `Corp`, `CORP` | `CORP` |

```yaml

intake_id: M0000449| Field | Input Examples | Normalize To || `enterprise_or_func_name` | `food`, `Food`, `FOOD` | `FOOD` |

bucket_name: prd-cmp4-corp-fin-scripts

bucket_description: For storing PySpark scripts for CORP FIN subgroup|-------|---------------|--------------|| `enterprise_or_func_name` | `spec`, `Spec`, `SPEC` | `SPEC` |

aws_account_id: '632247962242'

aws_region: us-east-1| `usage_type` | `source`, `SOURCE`, `src` | `Source` || `enterprise_or_func_subgrp_name` | `apac`, `Apac` | `APAC` |

usage_type: Scripts

enterprise_or_func_name: CORP| `usage_type` | `dataproduct`, `data product`, `dp`, `DP` | `DataProduct` || `enterprise_or_func_subgrp_name` | `hr`, `Hr` | `HR` |

enterprise_or_func_subgrp_name: FIN

versioning_enabled: true| `usage_type` | `scripts`, `script`, `SCRIPTS` | `Scripts` || `enterprise_or_func_subgrp_name` | `fin`, `Fin` | `FIN` |

```

| `usage_type` | `engassets`, `eng assets`, `engineering assets`, `eng-assets` | `EngAssets` || `encryption_type` | `sse-s3`, `sses3`, `s3`, `SSE_S3` | `SSE-S3` |

### Compute — EngAssets Bucket

```yaml| `enterprise_or_func_name` | `agtr`, `Agtr`, `ag trading` | `AGTR` || `encryption_type` | `sse-kms`, `ssekms`, `kms`, `SSE_KMS` | `SSE-KMS` |

intake_id: M0000516

bucket_name: prd-cmp1-apac-eng-assets| `enterprise_or_func_name` | `corp`, `Corp`, `corporate` | `CORP` || `encryption_type` | `sse-c`, `ssec`, `SSE_C` | `SSE-C` |

bucket_description: "Stores Engineering Assets for AGTR APAC"

aws_account_id: '367241115350'| `enterprise_or_func_name` | `food`, `Food`, `FOOD` | `FOOD` || `versioning_enabled` | `yes`, `Yes`, `YES`, `1`, `"true"` | `true` |

aws_region: us-east-1

usage_type: EngAssets| `enterprise_or_func_name` | `spec`, `Spec`, `specialized` | `SPEC` || `versioning_enabled` | `no`, `No`, `NO`, `0`, `"false"` | `false` |

enterprise_or_func_name: AGTR

enterprise_or_func_subgrp_name: APAC| `enterprise_or_func_subgrp_name` | `apac`, `Apac` | `APAC` || `public_access_blocked` | same as above | `true` / `false` |

```

| `enterprise_or_func_subgrp_name` | `hr`, `Hr` | `HR` || `encryption_enabled` | same as above | `true` / `false` |

| `enterprise_or_func_subgrp_name` | `fin`, `Fin` | `FIN` |

| `aws_region` | `us east 1`, `US-EAST-1`, `useast1` | `us-east-1` |General normalization: all enum fields are case-insensitive after normalization.



**CRITICAL: Normalize first, then validate. `Food` → `FOOD` → valid. Do NOT reject `Food`.**---



---## YAML Generation Rules



## YAML Generation Rules1. Field order: `intake_id` → `bucket_name` → `bucket_description` → `aws_account_id` → `aws_region` → `usage_type` → `enterprise_or_func_name` → `enterprise_or_func_subgrp_name` → security block

2. Always include `enterprise_or_func_subgrp_name` even if empty string

1. **Field order:** `intake_id` → `bucket_name` → `bucket_description` → `aws_account_id` → `aws_region` → `usage_type` → `enterprise_or_func_name` → `enterprise_or_func_subgrp_name` → (versioning if Scripts)3. Omit optional fields at default value EXCEPT the full security block — always include it

2. Always include `enterprise_or_func_subgrp_name` even if empty string.4. Omit `encryption_key_arn` if `encryption_type = SSE-S3`

3. **Do NOT include** `versioning_enabled`, `public_access_blocked`, `encryption_enabled`, `encryption_type`, or `encryption_key_arn` — UNLESS `usage_type = Scripts` (then include `versioning_enabled: true` only).5. Add `# Security Configuration` comment above security block

4. **Quoting rules:**6. **Quoting rules:**

   - `aws_account_id` → always single-quoted: `'438465132548'`   - `aws_account_id` → always single-quoted even though numeric e.g. `'438465132548'`

   - Strings with spaces → double quotes: `"Stores data for..."`   - Strings with spaces → double quotes

   - Plain strings (no spaces) → no quotes: `AGTR`   - ARNs → single quotes

   - Booleans → no quotes: `true` / `false`   - Plain strings (no spaces) → no quotes

   - Empty string → double quotes: `""`   - Booleans → no quotes (`true` / `false`, not `"true"`)

   - All other numbers → no quotes

---

---

## Templates

## Templates

### Lakehouse — Source Bucket

```yaml### Lakehouse — Source Bucket

intake_id: M0000452```yaml

bucket_name: prd-lh1-agtr-srcintake_id: I-123456

bucket_description: Stores MIF to lakehouse Ingestion pattern SAP TCC objectsbucket_name: dev-lh1-agtr-src

aws_account_id: '578647603827'bucket_description: "Stores source system data for Ag and Trading"

aws_region: us-east-1aws_account_id: '438465132548'

usage_type: Sourceaws_region: us-east-1

enterprise_or_func_name: AGTRusage_type: Source

enterprise_or_func_subgrp_name: APACenterprise_or_func_name: AGTR

```enterprise_or_func_subgrp_name: ""



### Lakehouse — EngAssets Bucket# Security Configuration

```yamlversioning_enabled: false

intake_id: M0000001public_access_blocked: true

bucket_name: prd-lh1-corp-dtd-eng-assetsencryption_enabled: true

bucket_description: "Stores Engineering Assets for CORP DTD"encryption_type: SSE-S3

aws_account_id: '578647603827'```

aws_region: us-east-1

usage_type: EngAssets### Compute — DataProduct Bucket with Subgroup

enterprise_or_func_name: CORP```yaml

enterprise_or_func_subgrp_name: DTDintake_id: M-0000485

```bucket_name: dev-cmp1-agtr-apac-dp

bucket_description: "Stores data product for Ag and Trading APAC"

### Lakehouse — Scripts Bucketaws_account_id: '068887784423'

```yamlaws_region: us-east-1

intake_id: M0000001usage_type: DataProduct

bucket_name: prd-lh1-corp-dtd-scriptsenterprise_or_func_name: AGTR

bucket_description: "Stores Scripts for CORP DTD"enterprise_or_func_subgrp_name: APAC

aws_account_id: '578647603827'

aws_region: us-east-1# Security Configuration

usage_type: Scriptsversioning_enabled: false

enterprise_or_func_name: CORPpublic_access_blocked: true

enterprise_or_func_subgrp_name: DTDencryption_enabled: true

versioning_enabled: trueencryption_type: SSE-S3

``````



### Compute — DataProduct Bucket### Scripts Bucket with Versioning

```yaml```yaml

intake_id: M0000449intake_id: M0000440

bucket_name: prd-cmp4-corp-fin-dpbucket_name: dev-lh1-corp-hr-scripts

bucket_description: Stores data product for Corporate Financebucket_description: "PySpark scripts for CORP HR subgroup"

aws_account_id: '632247962242'aws_account_id: '438465132548'

aws_region: us-east-1aws_region: us-east-1

usage_type: DataProductusage_type: Scripts

enterprise_or_func_name: CORPenterprise_or_func_name: CORP

enterprise_or_func_subgrp_name: FINenterprise_or_func_subgrp_name: HR

```

# Security Configuration

### Compute — Scripts Bucketversioning_enabled: true

```yamlpublic_access_blocked: true

intake_id: M0000449encryption_enabled: true

bucket_name: prd-cmp4-corp-fin-scriptsencryption_type: SSE-S3

bucket_description: For storing PySpark scripts for CORP FIN subgroup```

aws_account_id: '632247962242'

aws_region: us-east-1---

usage_type: Scripts
enterprise_or_func_name: CORP
enterprise_or_func_subgrp_name: FIN
versioning_enabled: true
```

### Compute — EngAssets Bucket
```yaml
intake_id: M0000516
bucket_name: prd-cmp1-apac-eng-assets
bucket_description: "Stores Engineering Assets for AGTR APAC"
aws_account_id: '367241115350'
aws_region: us-east-1
usage_type: EngAssets
enterprise_or_func_name: AGTR
enterprise_or_func_subgrp_name: APAC
```
